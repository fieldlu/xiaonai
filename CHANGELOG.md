# Changelog


本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 与
[Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.0.19] - 2026-09-11

### 新增
- **综测知识库检索路由增强**（`bridge.py` / `search/smart_search.py`）：适配新入库的
  2025-2026 学年综合素质测评知识库（汽车工程学院 2024 级）。
  - `bridge.py`：`_KB_EXACT_KW` 精确词表新增「综测 / 综合测评 / 加分细则 / 星级查寝 /
    竞赛目录 / 奖励分」——命中即跳过 LLM 查询改写、直接进入检索，避免改写丢失关键术语；
    MiMo 工具路由的 `kb` 工具描述补充「综测加分 / 星级查寝 / 奖助学金」场景。
  - `search/smart_search.py`：`INT_KW` 意图词表新增「综测 / 综合测评 / 测评 / 加分 /
    星级查寝 / 查寝 / 星级寝室」。
  - 实测：「综测素质拓展奖励分上限几分」「汽车学院这次星级查寝评了哪些等级」Top 命中
    均为综测知识库文档（可信度 95%）。

## [0.0.18] - 2026-09-11

### 修复
- **QQ 掉线探测缺口**（`scripts/health_check.sh`）：掉线检测原先只看 OneBot 响应信封
  `get_login_info` 的 `status` 字段，但 NapCat 在账号被踢下线时该字段**仍返回 `ok`**——
  真正的离线标志在 `get_status` 的 `data.online`。结果是掉线期间健康检查持续报告正常
  （实测一次 2 小时掉线，期间每 15 分钟一次检查全部 `exit=0`），自愈永不触发。
  - 改为探测 `get_status` 的 `online` 字段，返回 `true` / `false` / `unreachable` 三分支；
  - 去掉原有 `uptime > 7200` 前置条件（该条件使 2 小时内的掉线同样被跳过），
    改用 90 秒启动保护期，避免与开机流程抢跑；
  - 判定离线后重启 QQ 与 bridge，若快速登录失败（QQ 已作废登录令牌）则主动通知
    管理员并附二维码路径；判定 `unreachable` 时按原有「进程假死」路径处理。
  - 健康检查频率由每 15 分钟收紧至每 5 分钟。
- **离线时发送空转**（`bridge.py`）：`napcat_api` 在账号离线时仍会等待完整的 WebSocket
  超时（默认 10 秒）才失败，一批推送重试会累计浪费数十秒。新增在线态预检
  （`_qq_online_cached`，5 秒进程内缓存，探测失败视为未知而非离线），已知离线时立即
  返回 `{"status": "failed", "error": "qq_offline"}`，不再进入 WS 等待。

## [0.0.17] - 2026-09-07

### 变更
- **校园通知研究生受众屏蔽**（`campus/`）：本插件面向本科班群，面向在读研究生的通知（研究生竞赛、培养学籍、学位、导师事务等）不再推送。
  - 规则层黑名单新增「研究生 / 博士 / MPAcc」三词；豁免层（先于黑名单）新增 5 个本科生刚需短语：推免保研（免试攻读）、研究生支教团、考研入学考试交通管制、本科生缴费、本科生选修研究生课程——逐词全列核验零冲突。
  - 金标准与训练集同步翻转：gold 67 条 1→0（conf 标记 `flip_grad` 可追溯）、NB 训练集 69 条翻转（正例 497→428），训练分布与受众标准对齐。
  - 全量金标准回归：规则层 FP=0（FN 集合与上一版完全一致，零新增误杀）；端到端 acc 0.999（FP=1 为既有记录在案的边界项）；推送量 keep_rate 0.502→0.437。

## [0.0.16] - 2026-09-06

### 修复
- **提醒误路由根治（`reminder_parser.py` + `bridge.py`）**：教学通知等长文含「提醒」字样与多处日期时间时，`parse_reminder` 会把整篇通知误设成「某日某时」的定时提醒并拿全文尾部当内容，正文本身从未发出。`set` 分支新增三层防御：
  - 超长消息（≥120 字）不再进入提醒解析；
  - 含发布/转发意图词（发到群里、发布、转发、群发、发通知、整理一下等）的消息跳过提醒路由；
  - 提醒动词必须与时间线索同句（按句号级分句，逗号不分句以兼容「明天9点，记得提醒我」式表达；时间线索复用模块既有 6 个时间正则）；
  - `bridge.py` 旧路径 `_handle_reminder` 同套防御兜底，提醒内容改为只取时间所在句中其后的文字；
  - 清空 / 查看 / 删除提醒的管理指令不受影响。
- **「周X / 下周X」日期解析修复（`reminder_parser.py`）**：原实现未减当天星期基准——周日说「周五提醒我」会落到周四、说「下周一」会落到下周日。现 `周X` 解析为最近的未来该日（当天即该日则指今天），`下周X` 解析为下个自然周的该日。

## [0.0.15] - 2026-09-02

### 变更
- 校园通知过滤升级为「全量金标准回归 + 机器学习全量训练」（`campus/`）：
  - **全量金标准**：对综合信息网 1032 条收割全列逐条建人工金标准（`gold_full.json`，复核区 123 条逐条人工标注 + 铁词区高置信翻转 12 条）。基线回归证伪原「253 条标注集 acc 100%」——全量仅 acc 0.947 / FP 38 / FN 17，随即按 FP/FN 逐层归因修正规则层。
  - **规则层 FP 归零**：强白宽词误放样本配收网黑词（45+ 词，每词入表前全列核验零学生冲突）；财务处/后管处/医管办/网络中心移出默认放行（金标准证明其学生通知 100% 被强白词覆盖，摘表 19 个误推全消、零保留损失）；修复弱白「网络」截胡网络中心通知的层序陷阱；黑名单新增豁免机制（救回「学生优秀共产党员」评选）；强白补「封闭/叮叮车/公开课/安全教育」救援词；字符 NB 学不会的近重复行政文体（固定资产/大扫除/报修系统/核心数据库/合同管理系统等）改由规则层确定性收网。
  - **机器学习全量训练**：NB 训练集 303 → 1034 条（1032 条收割全列去重后 960 唯一标题全部入训 + 74 条历史补标），阈值 θ 2.5 → 1.5（真实部署池排除式重定标）。端到端全量评测 acc 0.999：规则层 FP=0 / NB 兜底 FN=0，唯一残留为一条两轮人工标注本身分歧的边界项（记录在案不掩盖）。
- 新增回归资产（`campus/`）：`gold_full.json`（1032 条金标准，含人工/翻转/采信分层）、`gold_rows.json`（分层明细）、`regress_full.py`（自带依赖 stub、改规则必跑的全量回归工具，本地与服务器输出一致）、`gztz_harvest.json`（1032 条收割语料）。

## [0.0.14] - 2026-09-02

### 新增
- 校园通知学生相关性过滤（`campus/`）：抓全综合信息网 gztz 公告列 29 页共 1032 条作回归语料，分层规则 + 本地朴素贝叶斯兜底的判定管线（硬黑名单 → 强白名单 → 纯行政科研部门否决 → 弱白名单 → 服务学生部门放行 → NB 兜底）。
- 数据驱动调优工具集（`campus/`）：`harvest_gztz.py`（分页收割）/ `eval_gztz.py`（全列评估）/ `verify_210.py`（标注回归）/ `flip_labels_passage.py`（误标翻转）/ `annotate_residual.py`（NB 边界样本标注）/ `tune_nb.py`（分层 5 折 CV 调参）/ `ablate_alpha.py`（α 部署消融）/ `sample_eval.py`（分层抽样复核）。

### 变更
- 校园通知规则层双向修正：基建处/保卫处/余区「临时封闭/占道/打围/交通管制/道路维修」等通行影响通知加入强白名单（先行于部门否决放行），其行政类仍被否决；「一号门」黑词移除（误伤余区校门封闭）；学工部进默认放行部门；黑名单补教职工福利/赛事/思政工作类词汇。
- 校园通知机器学习层升级：训练集 209 → 253 条（并入 29 条真实 NB 决策边界样本，消除训练/部署分布错位）；字符 gram (2,3) → (1,2,3)、alpha 1.0 → 0.5（抗行政培训近重复误捞），标注集 acc 100%（TP=105 / FN=0 / FP=0 / TN=148），1032 条全列 keep_rate 53.6%。
- 群图片回复策略：静默群改为严格 @ 制（表情/图片不再触发免@识图回复）；新增 90s 等待窗口（未@图片暂存，下一条提及「小奈」才识图回复，超时静默丢弃）。
- 模型默认端点切回小米官方按量付费（`api.xiaomimimo.com/v1`）。

## [0.0.12] - 2026-08-24

### 新增
- 附带脱敏示例知识库 `data/knowledge/school/`（243 个文件）：武汉理工大学公开校情（新生攻略、选课评价汇总、教师信息、学生手册要点、校级办法），部署后可立即体验检索。
- 知识库整理方法论沉淀：空壳文件清理、教师/课程评价碎片按主题合并、BM25 口语停用词扩充。

### 变更
- `.gitignore`：`data/knowledge/school/` 白名单纳入版本管理，其余运行时知识库仍忽略。

## [0.0.11] - 2026-08-18

### 修复
- 补齐“第 N 点/项”标题识别，避免多主题回复在重新分段时跨主题合并。
- 增加连续标题、短开场和数字标题格式的回归测试。

## [0.0.10] - 2026-08-18

### 修复
- 继续优化真实女友感的消息分段：修复短标题跨主题吞并、长段尾部堆积和分段长度不均的问题。
- 支持中文/ASCII 引号、括号跨行或跨空行时的安全合并，并在发送前将超长片段按句子或安全字符边界拆开。
- 增补酒店建议、亲密关系咨询、多主题短标题、超长文本和空输入回归测试。

## [0.0.9] - 2026-08-18

### 修复
- 优化 QQ 回复分段：不再机械地按每一行单独发送，短标题会和后续内容合并，超过 4 段时按语义顺序重新均衡打包，避免前几条很短、最后一条堆成长段。
- 保留引号换行合并、短回复单条发送和最多 4 段限制，并补充分段回归测试。
## [0.0.8] - 2026-08-18

### 修复
- 防止带内容的按日期取消误删同一天其他提醒，并统一非零填充日期格式。
- 进一步阻止带中文标点的引号解释文本、礼貌否定句和主语否定句误触发提醒设置。
- 规范化日期删除的后缀处理，避免把真实内容首字误当作日期后缀。

## [0.0.7] - 2026-08-18

### 修复
- 修复“取消所有定制提醒”无法命中清空命令的问题。
- 支持“取消 YYYY-MM-DD 的”这类按日期删除提醒的简写。
- 提醒解析只读取用户本轮原始文字，避免历史记忆或人格上下文中的日期/“提醒”误触发设置提醒。
- 支持“请帮我…”等连续礼貌前缀，并拒绝明显的解释性提问误触发设置提醒。
- 修复提醒 JSON 文件句柄未关闭问题，补充群聊作用域隔离回归测试。

### 测试
- 新增 `test_reminder_parser_regression.py`，覆盖清空、按日期取消和正常设置提醒。

## [0.0.6] - 2026-08-18

### 新增
- **第二阶段持续陪伴关系层**：为每个用户增加主动陪伴开关、安静时段、每日额度、20 小时冷却、称呼偏好、讨厌短语和有限关系事件记录。
- **明确的私聊控制命令**：支持“开启陪伴提醒”“关闭陪伴提醒”“陪伴状态”“叫我 X”“别这样叫我 X”“我不喜欢你说 X”，命令在 bridge 层直接处理，不交给 LLM 猜测。
- **时间感和连续性提示**：根据早晨、白天、晚上、深夜调整回复约束；私聊最多自然跟进一个当前用户自己的未完话题。

### 安全与隐私
- 主动陪伴默认关闭；群聊不能修改个人陪伴设置，也不会注入私聊称呼、偏好、事件或开放话题。
- 主动陪伴不使用催促回复、制造愧疚或“为什么不理我”等情感施压话术。
- 关系事件只保存短摘要，最多 20 条，不保存完整聊天原文；旧关系 JSON 缺字段时使用安全默认值迁移。

### 测试
- `python3 -m py_compile bridge.py xiaonai_memory.py src/memory/mood.py src/memory/relationship_state.py test_relationship_state.py test_phase2_relationship.py test_reply_segments.py`
- `python3 -m unittest -v test_relationship_state.py test_phase2_relationship.py test_reply_segments.py`
## [0.0.5] - 2026-08-18

### 新增
- **全用户真实女友式关系层**：为每个 QQ 用户独立保存关系阶段、互动温度、信任、亲密度、开放话题和修复状态，让称呼、记忆与共同经历不串台。
- **按场景调整表达**：新增温暖问候、日常陪伴、轻松打趣、安慰、害羞回应、边界修复和低能量等行为模式，并把情绪状态转换为可执行的表达约束。

### 修复
- **群聊隐私隔离**：群聊不再注入私聊昵称、QQ 号、私聊事实、关系阶段、私聊记忆或排他性表达；未完话题只在对应用户私聊中使用。
- **NapCat 重连稳定性**：HTTP API 8081 改为单实例生命周期，避免重连时重复绑定；旧 WebSocket 断开不会清空新连接状态。
- **关系状态数据健壮性**：增加文本清洗、异常情绪值容错、关系状态回归测试和群聊隐私回归断言。

### 测试
- `python3 -m py_compile ...`
- `python3 -m unittest -v test_relationship_state.py test_reply_segments.py`

## [0.0.4] - 2026-08-16

### 修复
- **识图后追问误注入知识库结果**（图片追问「详细解释一下」→ agent 反问「详细解释啥呀？」）：工具注入/路由原来基于带记忆/性格前缀的完整消息做正则判断，前缀里的「什么」「吗」等词命中 legacy `is_q` 正则，把追问误判为知识库查询，smart_search 注入无关结果（曾注入「校园网管理规定」775 chars）→ agent 被干扰。改为基于用户原始文字 `_user_raw_text` 判断（与 #43 识图描述污染提醒判断同型：系统注入文本不得参与用户意图判断）。已实测：前缀拼接后 is_q=True 误触发注入、用原始文字 is_q=False 不注入；正常工具路由（score/资源/知识库）零回归

## [0.0.3] - 2026-08-15

### 修复
- **群分类/群管理描述与行为对齐**（对照 bridge.py 真实行为全面校对）：
  - personality.py 群冒泡：CLASS_GROUP 变量取值与注释互相矛盾（变量名 CLASS 却取 CHAT 占位符、注释说闲聊群不冒泡）→ 改为 BUBBLE_GROUP = CHAT_GROUP_PLACEHOLDER，与 FEATURES-GUIDE「只发 chat_groups」一致
  - ai_handler.py 群管理 action：原 add_class_group/normal/mute/chat 四个动作全写进 class_groups、remove_* 全写进 chat_groups → 改为每个动作各归各键
  - ai_handler.py 黑名单：原按 session_id（群消息=群号）判断 → 改为按用户 QQ 判断（与 bridge 一致，文档明示按用户拉黑）
  - ai_handler.py 未配置群：原「被动观察+条件回复」→ 完全忽略（与 bridge 主链路一致）
  - client.py 群配置提示词：测试群描述修正、黑名单传参改为 QQ 号
  - SOUL.md 群清单：移除真实群名（敏感信息），描述对齐回复策略
  - TOOLS.md 测试群描述：明确 test_group 是默认发送目标、运维告警走 admin 私信
  - scheduler_v5.py 默认订阅：news 默认关闭（原默认开启会向班级群推送）、补 daily_greetings 键

## [0.0.2] - 2026-08-15

### 新增
- 开源发布准备：统一部署路径为 `/opt/xiaonai`，移除硬编码密钥与个人信息
- 新增 CI（GitHub Actions）：语法检查 + Ruff + 密钥泄露扫描
- 新增社区文件：CONTRIBUTING / SECURITY / CODE_OF_CONDUCT / issue & PR 模板
- 新增密钥扫描脚本 `scripts/scan_secrets.sh`
- **识图回复相关性兜底**：用 LLM 判断回复是否与识图描述相关，检测服务端缓存串台导致的跑题回复并自动重试（`_reply_relevance_check`）

### 修复
- 识图描述误触发定时提醒：`parse_reminder` 命中识图描述里的「定时」字样 → 新增 `_strip_vision_desc` 剥离识图描述块
- 识图偶发空返回：MiMo 返回空 content → vision 空返回自动重试一次
- 消息断句：引号被换行劈开 → `_has_unclosed_quote` 检测并合并
- 死循环防护：SOUL/AGENTS 禁止 exec 反复搜索知识库 + call_openclaw 三层防御（降超时/换内容/英文推理检测）
- router unparseable 分支补熔断

### 安全
- 移除硬编码的和风天气 API Key（改用 `.env` 注入，`QW_API_KEY` / `QW_API_HOST`）
- 移除真实同学姓名与 QQ 群号（替换为占位符）
- `.gitignore` 补充 `.webvpn_ticket` / `.resource_token` / `exports/`
- 删除独立 WebVPN 编码工具脚本（功能内联，降低被关注风险）

## [0.0.1] - 2026-08-15

首个公开版本基线（开源首发）。

### 核心能力
- 消息桥接：BATCH 合并、去重、安全过滤、分段发送
- 工具路由：9 种工具（成绩 / 招生计划 / 校园通知 / 资源 / 知识库 / 考试 / 论文 / 提醒 / 天气）
- 知识库：BM25 + 字符语义 TF-IDF 多路检索 + MiMo 查询改写
- 三层记忆：会话 / resume 摘要 / 长期笔记（SQLite）
- 好感度引擎：六维情感模型 + 雷达图
- 定时推送：天气 / 地震预警 / 校园通知 / 考试倒计时（零 LLM 依赖）
- 图片识图：MiMo 多模态
- 健康自愈：服务重启、会话锁清理、内存/磁盘监控、熔断器

### 运维
- systemd 单元文件（bridge / scheduler）
- `health_check.sh` cron 巡检
- `admin_cli.py` 运维 CLI
